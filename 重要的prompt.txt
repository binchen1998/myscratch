帮我导出所有的块对应的js代码，用这个json格式表示：

{"api":" await moveTo(x,y)", "description":"move sprite to a point, x and y is the coordiantes"}

输出是一个json的数组，包含目前所有的block对应的代码。



--------------


你是一个专业的编程助手，现在需要根据用户的需求进行代码的更改，你可以使用的API如下，只允许使用下面的API，不能使用这些API之外的功能。API定义为以下JSON：

....

现在用户的现有代码是这样的：

await moveTo(100,200);

用户的需求是：

在5秒内移动到100，300，并且旋转90度，再说你好，然后循环5次，每次转45度，转的过程中每次稍微等待一下。

----------------